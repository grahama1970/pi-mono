Spawn 3 subagents using the Agent tool. Run them concurrently.

Subagent 1:
Create file test-agent-1.txt with content "agent 1 was here".
git add test-agent-1.txt && git commit -m "test: agent 1"
Output the commit hash.

Subagent 2:
Create file test-agent-2.txt with content "agent 2 was here".
git add test-agent-2.txt && git commit -m "test: agent 2"
Output the commit hash.

Subagent 3:
Create file test-agent-3.txt with content "agent 3 was here".
git add test-agent-3.txt && git commit -m "test: agent 3"
Output the commit hash.

Report back: which agents committed successfully, which failed, and what each output.
