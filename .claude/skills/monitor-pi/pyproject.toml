[project]
name = "monitor-pi"
version = "1.0.0"
description = "Pi agent infrastructure health watchdog"
requires-python = ">=3.11"
dependencies = [
    "loguru",
    "typer",
    "httpx",
]

[dependency-groups]
dev = []
