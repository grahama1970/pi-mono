[project]
name = "monitor-workstation"
version = "1.0.0"
description = "Nightly workstation health monitor — NVMe artifacts, cache bloat, drive health"
requires-python = ">=3.11"
dependencies = [
    "loguru>=0.7.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "httpx>=0.24.0",
]
