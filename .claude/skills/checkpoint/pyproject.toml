[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "checkpoint"
version = "1.0.0"
description = "Session checkpoint storage and recall via /memory"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9",
    "rich>=13.0",
    "loguru>=0.7",
]
