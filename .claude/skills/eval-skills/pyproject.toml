[project]
name = "eval-skills"
version = "0.1.0"
description = "Behavioral evaluation of skills using fixture-based test cases"
requires-python = ">=3.11"

dependencies = [
    "typer>=0.9.0",
    "loguru>=0.7.0",
    "jsonschema>=4.0.0",
]
