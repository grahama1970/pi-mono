[project]
name = "monitor-claude"
version = "1.0.0"
description = "Continuous Claude Code process health watchdog"
requires-python = ">=3.11"
dependencies = [
    "loguru",
    "typer",
    "python-dotenv",
    "httpx",
]

[tool.uv]
dev-dependencies = []
