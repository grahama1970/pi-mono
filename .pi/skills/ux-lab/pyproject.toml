[project]
name = "ux-lab"
version = "0.1.0"
description = "Agent-controllable design canvas with React export"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
